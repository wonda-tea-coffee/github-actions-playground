name: Test YAML hash comment bug

on:
  workflow_dispatch:

permissions: {}

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Generate text with hash
        id: generate
        uses: actions/github-script@v7
        with:
          result-encoding: string
          script: |
            return `### patch-feat: 🎸 add CSV download #14620\r\nsome staging test content\r\n### minor-feat: 🎸 add new feature #14621\r\nmore content here`;

      # ケース1: クォートなし（バグ再現）
      - name: Without quotes
        env:
          BODY: ${{ steps.generate.outputs.result }}
        run: |
          echo "=== Without quotes ==="
          echo "$BODY"

      # ケース2: クォート付き（修正案）
      - name: With quotes
        env:
          BODY: "${{ steps.generate.outputs.result }}"
        run: |
          echo "=== With quotes ==="
          echo "$BODY"
